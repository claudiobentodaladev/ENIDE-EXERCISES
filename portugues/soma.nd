#incluir<portugues.lang>;

numero valorA = 0;
numero valorB = 0;
numero soma = 0;

escreva("Insere o primeiro valor:");
leia(numero,valorA);

escreva("Insere o segundo valor:");
leia(numero,valorB);

soma: numero = valorA + valorB;

escreva("soma", soma);