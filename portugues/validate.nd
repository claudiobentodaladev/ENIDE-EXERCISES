#incluir<portugues.lang>;

numero valor = 0;

escreva("Insere um numero entre 1 à 10:");
leia(numero, valor);

enquanto(valor < 0 | valor > 10){
    escreva("Insere um numero entre 1 à 10:");
    leia(numero, valor);
}

escreva("Obrigado por Inserir", valor);